{
  "name": "mcp-pool",
  "version": "0.1.0",
  "description": "MCP Key Pool Proxy — pool multiple API keys across upstream MCP servers",
  "type": "module",
  "bin": {
    "mcp-pool": "dist/index.js"
  },
  "files": [
    "dist/",
    "README.md"
  ],
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.0",
    "yaml": "^2.7.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "build": "tsc",
    "prepare": "tsc",
    "start": "node dist/index.js",
    "dev": "bun --watch src/index.ts",
    "test": "bun test src/"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/your-username/mcp-pool.git"
  },
  "keywords": ["mcp", "model-context-protocol", "proxy", "rate-limit", "api-key"],
  "license": "MIT"
}
