{
  "name": "mcp-pool",
  "version": "0.1.0",
  "description": "MCP Key Pool Proxy — pool multiple API keys across upstream MCP servers",
  "type": "module",
  "bin": {
    "mcp-pool": "src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.6.0",
    "yaml": "^2.7.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.7.0"
  },
  "scripts": {
    "build": "tsc",
    "start": "bun run dist/index.js",
    "dev": "bun --watch src/index.ts",
    "test": "bun test"
  }
}
